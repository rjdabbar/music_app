<h1>Create a new band!</h1>
  <%= flash_errors %>
  <form class="" action="<%= bands_url %>" method="post">
    <%= render partial: "partials/band_form", locals: { band: @band } %>
    <button>Create Band</button>
  </form>
