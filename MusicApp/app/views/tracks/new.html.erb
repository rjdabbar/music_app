<h1>Add a new track to <%= @track.album.name %></h1>

<%= flash_errors %>

<form action="<%= tracks_url %>" method="post">
  <%= render partial: "partials/track_form", locals: { track: @track } %>
  <button>Create Track</button>
</form>
